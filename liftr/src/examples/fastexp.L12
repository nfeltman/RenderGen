%% fast exponent

let rec exp ((b,e) : $int*int) : $int = 
	if e == 0 then 
		next{1} 
	else if (e mod 2) == 0 then
		exp (next{prev{b}*prev{b}},e/2)
	else next{prev{b} * prev{exp (b,e-1)}} 
in exp (next{3},5)