
-- module ObjParser where
-- import Data.List.Split
-- import Geometry

-- parseObj :: String -> [Tri3]
-- parseObj = 
    -- (foldl accum ([],[])).(splitOn "\n") where
        -- accum (vs,fs) line = if line == "" then 